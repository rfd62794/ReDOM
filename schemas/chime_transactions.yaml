source: chime_transactions
reference_date: "2026-06-09"
anchors:
  - selector: "h2"
    role: date_header
    resolve: relative_date
record_selector: "a.group.flex.flex-col"
inherits: date_header
fields:
  - name: description
    selector: ".flex.flex-row.gap-4 .flex.flex-col div:first-child"
    kind: string
  - name: amount
    selector: ".flex.flex-col.shrink-0 .text-label"
    kind: currency
on_unresolved: flag
