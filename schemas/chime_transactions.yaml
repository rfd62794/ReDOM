source: chime_transactions
reference_date: "2026-06-09"
anchors:
  - selector: "h2"
    role: date_header
    resolve: relative_date
record_selector: ".flex.flex-row.gap-4"
inherits: date_header
fields:
  - name: description
    selector: ".min-w-0 span"
    kind: string
  - name: amount
    selector: ".text-content-secondary, .text-content-action-primary"
    kind: currency
on_unresolved: flag
